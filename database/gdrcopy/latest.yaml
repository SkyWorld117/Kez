---
recipe:
    name: gdrcopy
    description: GDRCopy userspace library
    type: external

  # This package should not be built from source
  # Use the system package instead

    properties:
        lib: ${gdrcopy.prefix}/lib64
        libs:
            default: -lgdrapi
        include: ${gdrcopy.prefix}/include
