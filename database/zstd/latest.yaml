---
recipe:
    name: zstd
    description: Zstandard compression library
    type: system
    toolchain: make

    dependencies:
        - zlib
        - xz
        - lz4

    properties:
        lib: ${zstd.prefix}/lib
        include: ${zstd.prefix}/include
        libs:
            default: -lzstd
