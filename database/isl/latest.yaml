---
recipe:
    name: isl
    description: Integer Set Library for polyhedral computations
    type: system
    toolchain: autotools

    dependencies:
        - gmp

    properties:
        lib: ${isl.prefix}/lib
        libs:
            default: -lisl
        include: ${isl.prefix}/include
