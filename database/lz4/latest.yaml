---
recipe:
    name: lz4
    description: Extremely fast compression algorithm
    type: system
    toolchain: make

    properties:
        lib: ${lz4.prefix}/lib
        libs:
            default: -llz4
        include: ${lz4.prefix}/include
