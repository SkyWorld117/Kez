cheese:

  name: mpi
  type: abstract

  implementations:
    - openmpi
    - mpich
    - intel-oneapi-mpi

  build:
    configurations:
      options:
        - name: mpi_use_cuda
          description: Enable CUDA support
          user_configurable: true