---
recipe:
    name: rdma-core
    description: RDMA core userspace libraries and daemons
    type: external

  # This package should not be built from source
  # Use the system package instead

    properties:
        lib: ${rdma-core.prefix}/lib64
        libs:
            default: -lrdmacm
        include: ${rdma-core.prefix}/include
