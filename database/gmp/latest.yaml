---
recipe:
    name: gmp
    description: GNU Multiple Precision Arithmetic Library
    type: system
    toolchain: autotools

    properties:
        lib: ${gmp.prefix}/lib
        libs:
            default: -lgmp
        include: ${gmp.prefix}/include
