---
recipe:
    name: hcoll
    description: Modern Interface for Mellanox Fabric Collective Accelerator
    type: external

  # This package should not be built from source
  # Use the system package instead

    properties:
        lib: ${hcoll.prefix}/lib
        libs:
            default: -lhcoll
        include: ${hcoll.prefix}/include
