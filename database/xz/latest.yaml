---
recipe:
    name: xz
    description: XZ Utils data compression software with high compression ratio
    type: system
    toolchain: autotools

    properties:
        lib: ${xz.prefix}/lib
        libs:
            default: -llzma
        include: ${xz.prefix}/include
