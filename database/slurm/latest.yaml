---
recipe:
    name: slurm
    description: Simple Linux Utility for Resource Management
    type: external

  # This package should not be built from source
  # Use the system package instead

    properties:
        lib: ${slurm.prefix}/lib64
        libs:
            default: -lslurm
        include: ${slurm.prefix}/include
