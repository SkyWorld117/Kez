---
recipe:
    name: xtrans
    description: X Transport library
    type: package
    toolchain: autotools

    source:
        type: tarball
        releases:
            - version: 1.6.0
              url: https://www.x.org/releases/individual/lib/xtrans-1.6.0.tar.xz

    dependencies:
        - util-macros

    build:
        configurations: {}
        stages:
            - target:
            - target: install

    properties:
        include: ${xtrans.prefix}/include
