---
recipe:
    name: mpc
    description: GNU MPC Library for multiple precision complex arithmetic
    type: system
    toolchain: autotools

    dependencies:
        - gmp
        - mpfr

    properties:
        lib: ${mpc.prefix}/lib
        libs:
            default: -lmpc
        include: ${mpc.prefix}/include
